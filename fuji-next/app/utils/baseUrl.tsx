export class BaseURL {

    static base = 'http://localhost:8080/api';
    static baseImage = 'http://localhost:8080';
    static fruit = `${BaseURL.base}/fruits`;
    static category = `${BaseURL.base}/categories`;
    static user = `${BaseURL.base}/users`;
    static auth = `${BaseURL.base}/auth`;
    static cart = `${BaseURL.base}/cart`;
    static order = `${BaseURL.base}/orders`;
    static news = `${BaseURL.base}/news`;
    static statitic = `${BaseURL.base}/statistics`;
    static vnpay = `${BaseURL.base}/vnpay`;
  }

